[project]
name = "op-bot"
version = "0.1.0"
description = "Safe BotoHub task delivery with an inline-only admin UI"
requires-python = ">=3.11"
dependencies = ["aiogram>=3.0"]

[project.optional-dependencies]
test = ["pytest>=8.0", "pytest-asyncio>=0.23"]

[tool.pytest.ini_options]
pythonpath = ["src"]
